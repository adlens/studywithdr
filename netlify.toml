# Netlify: publish entire repo root (no build)
[build]
  publish = "."

# No redirects – serve all files as-is
# (removes any default SPA-style redirect)

[[redirects]]
  from = "/games/memory-match/dist/*"
  to = "/games/memory-match/:splat"
  status = 301
